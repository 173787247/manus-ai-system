# 快速查看输出指南

## 🎯 已执行的功能

### 测试覆盖的27个功能：

1. **基础智能体** (5个功能)
   - 初始化、数据处理、状态管理、记忆管理、统计信息

2. **智能体管理器** (5个功能)
   - 消息总线、智能体管理、任务协调

3. **规划智能体** (4个功能)
   - 任务理解、任务分解、依赖分析、执行规划

4. **知识检索** (4个功能)
   - 向量检索、关键词检索、知识图谱查询、结果融合

5. **GUI操作** (4个功能)
   - 动作解析、动作验证、动作执行

6. **任务执行器** (3个功能)
   - 任务执行流程、错误处理、结果返回

7. **集成测试** (2个功能)
   - 完整任务流程、智能体协作

---

## 📍 输出在哪里看

### 方法1: 控制台输出（最简单）⭐

```bash
# 运行测试，直接在控制台查看
python run_tests_fixed.py
```

**输出位置**: 控制台窗口  
**输出内容**: 
- 每个测试的执行状态
- PASSED/FAILED 标记
- 测试统计信息

**示例**:
```
[PASS]: tests/test_base_agent.py
[PASS]: tests/test_agent_manager.py
...
Total: 7/7 passed
[SUCCESS] All tests passed!
```

---

### 方法2: 详细测试输出

```bash
# 查看详细的测试输出
python -m pytest tests/ -v
```

**输出位置**: 控制台  
**输出内容**: 
- 每个测试用例的详细执行过程
- 测试通过/失败信息
- 执行时间

**示例**:
```
tests/test_base_agent.py::test_base_agent_initialization PASSED
tests/test_base_agent.py::test_base_agent_process PASSED
...
======================== 5 passed in 0.07s =============================
```

---

### 方法3: 保存到文件

```bash
# 保存输出到文件
python run_tests_fixed.py > test_output.txt 2>&1

# 查看文件
type test_output.txt
```

**输出位置**: `test_output.txt`（项目根目录）  
**优点**: 可以保存和查看历史记录

---

### 方法4: HTML报告（需要安装插件）

```bash
# 1. 先安装插件
pip install pytest-html

# 2. 生成HTML报告
python -m pytest tests/ --html=test_report.html --self-contained-html

# 3. 打开报告
start test_report.html
```

**输出位置**: `test_report.html`（项目根目录）  
**优点**: 可视化，易于查看

---

### 方法5: 覆盖率报告

```bash
# 生成覆盖率报告
python -m pytest tests/ --cov=src --cov-report=html

# 打开报告
start htmlcov\index.html
```

**输出位置**: `htmlcov/index.html`  
**内容**: 代码覆盖率详情

---

## 🔍 查看特定功能的输出

### 查看基础智能体功能

```bash
python -m pytest tests/test_base_agent.py -v
```

**输出**: 5个测试用例的执行结果

---

### 查看规划智能体功能

```bash
python -m pytest tests/test_planning_agent.py -v
```

**输出**: 4个测试用例的执行结果，包括任务分解过程

---

### 查看知识检索功能

```bash
python -m pytest tests/test_knowledge_agent.py -v
```

**输出**: 4个测试用例的执行结果，包括检索过程

---

### 查看GUI操作功能

```bash
python -m pytest tests/test_gui_agent.py -v
```

**输出**: 4个测试用例的执行结果，包括动作解析过程

---

### 查看完整任务执行

```bash
python -m pytest tests/test_integration.py -v -m integration
```

**输出**: 2个集成测试的执行结果，包括完整任务流程

---

## 📊 输出内容说明

### 测试输出包含：

1. **测试执行信息**
   - 测试文件路径
   - 测试函数名
   - 执行状态（PASSED/FAILED）

2. **测试统计**
   - 总测试数
   - 通过数
   - 失败数
   - 执行时间

3. **错误信息**（如果有）
   - 错误类型
   - 错误位置
   - 错误详情

---

## 🚀 快速查看命令

### 最简单的方式

```bash
# 直接运行，查看控制台输出
python run_tests_fixed.py
```

### 查看详细输出

```bash
# 显示详细测试信息
python -m pytest tests/ -v -s
```

### 保存输出到文件

```bash
# 保存所有输出
python run_tests_fixed.py > output.txt 2>&1
type output.txt
```

---

## 📂 输出文件位置

| 输出类型 | 文件位置 | 查看命令 |
|---------|---------|---------|
| 控制台输出 | 控制台窗口 | `python run_tests_fixed.py` |
| 测试输出文件 | `test_output.txt` | `type test_output.txt` |
| HTML报告 | `test_report.html` | `start test_report.html` |
| 覆盖率报告 | `htmlcov/index.html` | `start htmlcov\index.html` |
| 日志文件 | `logs/manus.log` | `type logs\manus.log` |

---

## 💡 推荐查看方式

### 开发调试时
```bash
python -m pytest tests/ -v -s
```
- 实时查看
- 详细输出
- 立即反馈

### 查看测试结果
```bash
python run_tests_fixed.py
```
- 简洁明了
- 快速查看
- 统计信息

### 保存测试记录
```bash
python run_tests_fixed.py > test_output.txt 2>&1
```
- 保存历史
- 便于回顾
- 文档记录

---

## 📋 功能执行清单

运行以下命令查看每个功能的执行结果：

```bash
# 1. 基础智能体 (5个功能)
python -m pytest tests/test_base_agent.py -v

# 2. 智能体管理器 (5个功能)
python -m pytest tests/test_agent_manager.py -v

# 3. 规划智能体 (4个功能)
python -m pytest tests/test_planning_agent.py -v

# 4. 知识检索 (4个功能)
python -m pytest tests/test_knowledge_agent.py -v

# 5. GUI操作 (4个功能)
python -m pytest tests/test_gui_agent.py -v

# 6. 任务执行器 (3个功能)
python -m pytest tests/test_task_executor.py -v

# 7. 集成测试 (2个功能)
python -m pytest tests/test_integration.py -v -m integration
```

每个命令都会显示该模块所有功能的执行结果和输出。

---

## ✅ 总结

**已执行的功能**: 27个核心功能，26个测试用例全部通过

**查看输出的最简单方法**:
```bash
python run_tests_fixed.py
```

**查看详细功能清单**: 查看 `FUNCTIONALITY_CHECKLIST.md`

**查看输出位置说明**: 查看 `VIEW_OUTPUT.md`

